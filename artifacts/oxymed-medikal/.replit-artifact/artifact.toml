kind = "web"
previewPath = "/"
title = "Oxymed Medikal"
version = "1.0.0"
id = "artifacts/oxymed-medikal"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23646

[services.development]
run = "pnpm --filter @workspace/oxymed-medikal run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/oxymed-medikal", "run", "build" ]
publicDir = "artifacts/oxymed-medikal/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23646"
BASE_PATH = "/"
